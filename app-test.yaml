runtime: python37
entrypoint: python main.py
service: tgif-test

handlers:
  - url: /(.*\..+)$
    static_files: build/\1
    upload: build/(.*\..+)$
  - url: /.*
    script: auto

manual_scaling:
  instances: 1

runtime: python37
entrypoint: python main.py
service: tgif-dev

handlers:
  - url: /(.*\..+)$
    static_files: build/\1
    upload: build/(.*\..+)$
  - url: /.*
    script: auto

manual_scaling:
  instances: 1
